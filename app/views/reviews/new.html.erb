<div class="container">
  <h1>New Review for <%= @serie.title %></h1>
  <%= simple_form_for [@serie, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit class: 'btn btn-warning' %>
  <% end %>
    <%= link_to "back " , serie_path(@serie), class: 'btn btn-warning btn-sm my-2' %>
</div>
